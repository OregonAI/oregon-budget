# SERVE + VALIDATE. This is what the Docker image installs and what the MCP server needs.
#
# The mirror/analysis stack lives in requirements-build.txt and is deliberately NOT here:
# the MCP server never reads the Parquet. Actuals are proxied live at ~600 ms with an
# executed_at envelope, so the deploy host needs neither pyarrow nor duckdb, and putting
# them in the image would ship ~60 MB of wheels that no request ever touches.

# corpus_toolkit CLIs (corpus-validate-frontmatter, corpus-verify-provenance, ...) and the
# MCP server. The [mcp] extra installs the SDK that `corpus-mcp-serve` imports; without it
# the CLI is on PATH but fails at import, so the container cannot serve.
#
# v1.6.0 or later is REQUIRED, not merely preferred: this corpus sets
# plugins.tools_module, which nothing before 1.6.0 reads. An older toolkit would ignore the
# key silently and serve a server with no list_datasets, query_dataset or join_lookup — the
# exact failure the seam's fatal-on-error behaviour exists to prevent, reintroduced by a
# stale pin.
corpus-toolkit[mcp,semantic] @ git+https://github.com/OregonAI/corpus-toolkit@v1.27.0
pyyaml
jsonschema
